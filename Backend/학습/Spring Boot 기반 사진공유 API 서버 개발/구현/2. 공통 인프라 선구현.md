# 📦 Picshare API 서버 – 실무 표준 패키지 구조 
> 기준
> 
> - 도메인 중심 구조 (User / Post / Comment / Like / Auth / Upload)
>     
> - 공통 인프라는 `common`, 환경/보안은 `config`
>     
> - Controller / Service / Repository / Entity / DTO 명확 분리
>     

```
com.cat.picshare
├── PicshareApplication.java          # Spring Boot 엔트리 포인트
│
├── config                            # 환경/보안/공통 설정
│   ├── security
│   │   ├── SecurityConfig.java       # Spring Security 설정
│   │   ├── JwtAuthenticationFilter.java
│   │   ├── JwtTokenProvider.java
│   │   └── AuthenticationEntryPointImpl.java
│   ├── jpa
│   │   └── JpaConfig.java            # JPA Auditing, 설정
│   └── web
│       └── WebConfig.java            # CORS, Converter 등
│
├── common                            # 전 도메인 공통 사용
│   ├── response
│   │   ├── ApiResponse.java          # 공통 성공/실패 응답 래퍼
│   │   └── ApiError.java             # 에러 응답 객체
│   ├── exception
│   │   ├── ErrorCode.java            # 전체 에러 코드 enum
│   │   ├── CustomException.java      # 비즈니스 예외
│   │   └── GlobalExceptionHandler.java
│   ├── entity
│   │   └── BaseTimeEntity.java       # createdAt / updatedAt
│   └── util
│       └── SecurityUtil.java         # 인증 사용자 조회 등 유틸
│
├── auth                              # 인증/인가 도메인
│   ├── controller
│   │   └── AuthController.java       # 로그인/회원가입/토큰
│   ├── service
│   │   └── AuthService.java
│   └── dto
│       ├── LoginRequest.java
│       └── SignupRequest.java
│
├── user                              # 사용자 도메인
│   ├── controller
│   │   └── UserController.java       # /users/me
│   ├── service
│   │   └── UserService.java
│   ├── repository
│   │   └── UserRepository.java
│   ├── entity
│   │   └── User.java
│   └── dto
│       └── UserMeResponse.java
│
├── post                              # 게시물 도메인
│   ├── controller
│   │   └── PostController.java
│   ├── service
│   │   └── PostService.java
│   ├── repository
│   │   └── PostRepository.java
│   ├── entity
│   │   └── Post.java
│   └── dto
│       ├── PostCreateRequest.java
│       ├── PostUpdateRequest.java
│       └── PostResponse.java
│
├── comment                           # 댓글 도메인
│   ├── controller
│   │   └── CommentController.java
│   ├── service
│   │   └── CommentService.java
│   ├── repository
│   │   └── CommentRepository.java
│   ├── entity
│   │   └── Comment.java
│   └── dto
│       └── CommentCreateRequest.java
│
├── like                              # 좋아요 도메인
│   ├── service
│   │   └── LikeService.java
│   ├── repository
│   │   └── PostLikeRepository.java
│   └── entity
│       └── PostLike.java
│
├── upload                            # 파일 업로드
│   ├── controller
│   │   └── UploadController.java
│   └── service
│       └── UploadService.java
│
└── scheduler                         # 스케줄러 작업
    └── CleanupScheduler.java
```

---

## 이 구조를 쓰는 이유 (현업 관점)

- **도메인 기준 분리**
    
    - 기능 하나 추가 시 “어디에 코드 넣을지” 고민이 사라짐
        
- **Controller / Service / Repository 역할 명확**
    
    - 테스트/리팩토링/협업 용이
        
- **공통 인프라와 도메인 코드 완전 분리**
    
    - 공통 응답/예외 수정해도 도메인 영향 없음
        
- **보안/설정 코드가 도메인 코드와 섞이지 않음**
    
    - Security, JPA 설정은 항상 `config`
        

이 구조는 **스타트업·중견·대기업 Spring 백엔드에서 전부 쓰는 평균값**이다.

---

# Chapter 2. 공통 인프라 선구현 (최종 정리본)

## 2.1 Profiles 기반 설정 파일 구성

### `application.yml` (공통) `application.properties` **삭제**
파일 경로: `src/main/resources/application.yml`
```yaml
spring:                      # Spring 설정 루트
  profiles:                  # 실행 환경(profile) 설정
    active: local             # 기본 실행 환경을 local로 지정
server:                      # 서버 설정 루트
  port: 8080                 # 서버 실행 포트
```

---

### `application-local.yml` (로컬 개발)
파일 경로: `src/main/resources/application-local.yml`
```yaml
spring:                                      # Spring 설정 루트
  datasource:                                # DB 연결 설정
    url: jdbc:h2:mem:testdb                  # H2 인메모리 DB URL
    driver-class-name: org.h2.Driver         # H2 JDBC 드라이버
    username: sa                             # H2 기본 사용자
    password:                               # H2 기본 비밀번호(로컬)
  jpa:                                      # JPA 동작 설정
    hibernate:                              # Hibernate 설정 그룹
      ddl-auto: create                      # 실행 시 테이블 자동 생성
    show-sql: true                          # SQL 콘솔 출력
    properties:                             # 추가 Hibernate 속성
      hibernate:                            # Hibernate 네임스페이스
        format_sql: true                    # SQL 포맷팅 출력
  h2:                                       # H2 전용 설정
    console:                                # H2 콘솔 설정
      enabled: true                         # H2 웹 콘솔 활성화
```

---

### `application-prod.yml` (운영 뼈대)
파일 경로: `src/main/resources/application-prod.yml`
```yaml
spring:                                      # Spring 설정 루트
  datasource:                                # 운영 DB 연결 설정
    url: ${DB_URL}                           # DB URL(환경변수)
    username: ${DB_USERNAME}                 # DB 사용자(환경변수)
    password: ${DB_PASSWORD}                 # DB 비밀번호(환경변수)
  jpa:                                      # JPA 동작 설정
    hibernate:                              # Hibernate 설정 그룹
      ddl-auto: validate                    # 스키마 변경 없이 검증만
```

---

## 2.2 Spring Security 기본 로그인 제거

### 목적 요약

- HTML 로그인 화면 제거
    
- 세션 기반 인증 제거
    
- **API 서버 + JWT 인증 전제 상태**로 만들기
    

---

### `SecurityConfig.java (기존 파일 수정)
파일 경로: `src/main/java/com/cat/picshare/config/security/SecurityConfig.java`
```java
package com.cat.picshare.config.security; // 보안 설정 패키지

import org.springframework.context.annotation.Bean; // Bean 등록 어노테이션
import org.springframework.context.annotation.Configuration; // 설정 클래스 표시
import org.springframework.security.config.annotation.web.builders.HttpSecurity; // HTTP 보안 설정 빌더
import org.springframework.security.config.http.SessionCreationPolicy; // 세션 정책 enum
import org.springframework.security.web.SecurityFilterChain; // 보안 필터 체인 타입
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter; // 기본 인증 필터 기준점

@Configuration // Spring 설정 클래스
public class SecurityConfig { // 보안 공통 설정 클래스

    @Bean // SecurityFilterChain Bean 등록
    public SecurityFilterChain securityFilterChain(HttpSecurity http, JwtTokenProvider tokenProvider) throws Exception { // 보안 규칙 정의
        JwtAuthenticationFilter jwtFilter = new JwtAuthenticationFilter(tokenProvider); // JWT 인증 필터 생성
        AuthenticationEntryPointImpl entryPoint = new AuthenticationEntryPointImpl(); // 401 JSON 응답 엔트리포인트 생성

        http.csrf(csrf -> csrf.disable()); // CSRF 비활성화(API 서버는 보통 사용 안 함)
        http.sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS)); // 세션 미사용(JWT는 무상태)
        http.formLogin(form -> form.disable()); // 기본 로그인 폼 비활성화(로그인 화면 제거)
        http.httpBasic(basic -> basic.disable()); // HTTP Basic 인증 비활성화

        http.exceptionHandling(ex -> ex.authenticationEntryPoint(entryPoint)); // 인증 실패 시 401을 JSON으로 반환

        http.authorizeHttpRequests(auth -> auth // 요청별 인가 설정
            .requestMatchers("/api/auth/**").permitAll() // 회원가입/로그인 API는 인증 없이 허용(필수)
            .requestMatchers("/api/test/custom").permitAll() // 커스텀 예외 테스트는 허용(학습용)
            .requestMatchers("/h2-console/**").permitAll() // H2 콘솔 허용(로컬 편의)
            .anyRequest().authenticated() // 그 외 모든 요청은 인증 필요(기본 정책: 닫기)
        );

        http.addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class); // JWT 필터를 인증 앞단에 배치

        http.headers(headers -> headers.frameOptions(frame -> frame.sameOrigin())); // H2 콘솔 프레임 허용(로컬용)

        return http.build(); // 보안 필터 체인 생성
    } // 메서드 종료
} // 클래스 종료
```

---

## 2.3 공통 응답 구조

### `ApiError.java`
파일 경로: `src/main/java/com/cat/picshare/common/response/ApiError.java`

```java
package com.cat.picshare.common.response;                  // 공통 응답 패키지

import com.fasterxml.jackson.annotation.JsonInclude;       // JSON 직렬화 설정

@JsonInclude(JsonInclude.Include.NON_NULL)                 // null 필드 JSON 제외
public class ApiError {                                    // 에러 응답 객체
    private final String code;                              // 에러 코드
    private final String message;                           // 에러 메시지
    private final Object details;                           // 추가 정보

    public ApiError(String code, String message, Object details) { // 전체 필드 생성자
        this.code = code;                                   // 코드 초기화
        this.message = message;                             // 메시지 초기화
        this.details = details;                             // 상세 정보 초기화
    }

    public static ApiError of(String code, String message) { // details 없는 생성
        return new ApiError(code, message, null);           // details=null
    }

    public static ApiError of(String code, String message, Object details) { // details 포함 생성
        return new ApiError(code, message, details);        // 전체 필드 설정
    }

    public String getCode() { return code; }                // code getter
    public String getMessage() { return message; }          // message getter
    public Object getDetails() { return details; }          // details getter
}
```

---

### `ApiResponse.java`
파일 경로: `src/main/java/com/cat/picshare/common/response/ApiResponse.java`
```java
package com.cat.picshare.common.response;                  // 공통 응답 패키지

import com.fasterxml.jackson.annotation.JsonInclude;       // JSON 직렬화 설정

@JsonInclude(JsonInclude.Include.NON_NULL)                 // null 필드 JSON 제외
public class ApiResponse<T> {                               // 공통 응답 래퍼
    private final boolean success;                          // 성공 여부
    private final T data;                                   // 성공 데이터
    private final ApiError error;                           // 실패 에러

    private ApiResponse(boolean success, T data, ApiError error) { // 생성자
        this.success = success;                              // 성공 여부 설정
        this.data = data;                                    // 데이터 설정
        this.error = error;                                  // 에러 설정
    }

    public static <T> ApiResponse<T> ok(T data) {            // 성공 응답(데이터)
        return new ApiResponse<>(true, data, null);          // success=true
    }

    public static ApiResponse<Void> ok() {                   // 성공 응답(데이터 없음)
        return new ApiResponse<>(true, null, null);          // 빈 성공 응답
    }

    public static ApiResponse<Void> fail(ApiError error) {   // 실패 응답
        return new ApiResponse<>(false, null, error);        // success=false
    }

    public boolean isSuccess() { return success; }           // success getter
    public T getData() { return data; }                      // data getter
    public ApiError getError() { return error; }             // error getter
}
```

---

## Chapter 2 종료 상태 체크
http://localhost:8080
- 서버 실행 성공
- 로그인 화면
- `/` 접속 시 404 또는 Whitelabel ✅
- 공통 응답 구조 준비 완료